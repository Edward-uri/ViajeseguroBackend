# Progreso — Plan B Tiempo Real (Socket.IO + FCM)

Plan: docs/superpowers/plans/2026-06-20-viajes-tiempo-real.md
Restricción dura: NINGÚN comando git (no add/commit/push). El usuario commitea.
Ejecución: subagent-driven (un implementador por task).

## Estado de tasks
- Task 1: completo (deps instaladas, env SOCKET_CORS_ORIGIN + FCM_SERVICE_ACCOUNT, typecheck limpio)
- Task 2: completo (puerto IEventoViajeNotifier nuevas firmas, 3 casos de uso + mock, flow.test.ts 7/7)
- Task 3: completo (src/realtime/rooms.ts + test, 1/1)
- Task 4: completo (SocketEventoViajeNotifier + test, 6/6, typecheck limpio)
- Task 5: completo (guardarUbicacion repo + registrarUbicacion use case + test, 4/4)
- Task 6: completo (FCM: tokensActivosDeUsuario, Fcm/Log/pickPushSender, test 4/4). NOTA: firebase-admin v14 -> imports nombrados 'firebase-admin/app' y 'firebase-admin/messaging' (no default/namespace). Comportamiento idéntico al plan.
- Task 7: completo (dependencies.ts: socketNotifier + pickPushSender + registrarUbicacion; mocks borrados; flow.test.ts 7/7)
- Task 8: completo (socketServer.ts auth+joins+eventos entrantes; socketAuth.test.ts 3/3)
- Task 9: completo (index.ts bootstrap http+io+attach+shutdown; realtime.test.ts E2E 1/1; suite completa 19 archivos / 64 tests verde; typecheck limpio)
- Final review: completo (opus). Sin Critical. Important 1-3 + nit 5 -> fix subagent en background. Important 4 (fuga cross-municipio) documentado como deuda en ADR-013 (decisión del usuario).
- Fix review: COMPLETO. Important 1-3 + nit 5 aplicados (shutdown async, cleanup tests, guards, getApps).
- Important 4: CERRADO. conductor:online deriva municipio del server (conductores.id_municipio vía conductorUseCases.municipioOperativo), valida vs cliente, ack {ok}. + zod en payloads entrantes (src/realtime/schemas.ts) + eventos tipados (src/realtime/events.ts). Nuevos: events.ts, schemas.ts, conductores/application/municipioOperativo.ts. Test negativo agregado.
- Estado final: typecheck limpio, suite 19 archivos / 66 tests verde. Todo en working tree (sin commitear; el usuario commitea).

# Plan C — Flujo del conductor (subagent-driven)
Plan: docs/superpowers/plans/2026-06-20-viajes-conductor.md
- Task 1 (disponibilidad: migración 007 + REST): COMPLETO. Review aplicado (rango geográfico zod + asserts null). 5/5 tests, typecheck limpio.
- Task 2 (viajes conductor: pendientes + asignados): COMPLETO. Review aplicado (test 403 /asignados). 
- Cierre Plan C: suite 22 archivos / 81 tests verde, typecheck limpio. Todo en working tree (sin commitear).
- Deuda preexistente señalada (Plan A): ViajeSchema en openapi omite las fechas (fechaSolicitud, etc.) — afecta todos los endpoints de viajes.

# Auth con contraseña (subagent-driven, sin commits)
Plan: docs/superpowers/plans/2026-06-20-auth-password.md
- Task 1 (migración 008 + tienePassword + POST /api/auth/password): COMPLETO. Review limpio (Spec ✅, calidad Aprobada). 4/4 tests verde, typecheck limpio. Sin commitear (working tree).
  - Minor para el review final: M-2 los tests de política usan 'abc' (viola 3 reglas a la vez); podría cubrirse cada regla por separado.
- Task 2 (POST /api/auth/login/password + passwordHashPorId): COMPLETO. Review limpio (Spec ✅, calidad Aprobada). 8/8 en el archivo, suite completa 89/89, typecheck limpio. Sin commitear (working tree).
  - Minor para el review final: el tipo inline de `dispositivo` en loginPassword es `string | null | undefined` vs `string | undefined` del schema (cosmético, sin bug).
- Review final (opus): sin Critical. Important I-1 (code del error de credenciales): CERRADO con opción A — CredencialesError ahora emite `code: 'CREDENCIALES'` (extiende AppError), mensaje 'Correo o contraseña inválidos', unificado OTP+password. Tests refuerzan `error.code`. Suite 89/89, typecheck limpio.
- Estado: feature COMPLETA, en working tree (sin commitear; el usuario hace push+deploy). Requiere migración 008 en prod.

# Admin de Tarifas (subagent-driven, sin commits)
Plan: docs/superpowers/plans/2026-06-20-admin-tarifas-zonas.md
- Task 1 (GET lista + POST crear zona+tarifa en transacción): COMPLETO. Review limpio (Spec ✅, calidad Aprobada). 8/8 tests, typecheck limpio. Sin migración nueva (reusa 006). Working tree.
  - Minor (no bloqueante): #1 el 404 de municipio inexistente depende de la FK zonas.id_municipio (existe en 006, test pasa); #2 ActualizarZonaSchema se agregó en Task 1 por pedido del plan (se consume en Task 2).
- Task 2 (PATCH editar + DELETE soft-delete): COMPLETO. Review: Spec ✅. 16/16 en el archivo, suite completa 105/105, typecheck limpio. Working tree.
  - Hallazgos adjudicados (no bloqueantes): I-1 mapRow(rows[0]!) no alcanzable por la invariante zona+tarifa (guardarlo=YAGNI); I-2 desactivar con pool.query es un solo UPDATE atómico, plan-aligned, no defecto; I-3 re-lectura sin re-filtrar municipio es segura por el FOR UPDATE previo. Pasados al review final para triage.
- Review final (opus): LISTO PARA MERGE. Sin Critical/Important. Concordó con I-1/I-2/I-3. Confirmó que uq_tarifa_zona_vigente no se viola (update en sitio).
  - N-1 (decisión de producto, no bloqueante): no se puede poner el centro (lat/lng) de una zona a NULL una vez seteado. Recomendado aceptar para MVP (cercanía es futuro). Fix de 1 línea si se quisiera (schema nullable + propagar null).
  - Minors: N-3 zona sin centro es invisible a zonaMasCercana (coherente); N-4 mensaje de error con comillas vacías (cosmético, no alcanzable); N-5 triple app.use('/api/admin') es deuda preexistente.
- Estado: feature COMPLETA, en working tree (sin commitear). Sin migración nueva. Suite 105/105.

# Contraseña opcional en el registro (subagent-driven, sin commits)
Plan: docs/superpowers/plans/2026-06-20-password-en-registro.md
- Task única (password? en register/complete, hash atómico en createUserWithPersona, reusa passwordPolitica+hashPassword): COMPLETO. Review limpio (Spec ✅, calidad Aprobada). 11/11 en password.test.ts, suite completa 108/108, typecheck limpio. Sin migración (reusa password_hash de 008). Working tree.
  - Minors (no bloqueantes): password?: string|null vs string del schema (sigue la convención del resto de CompleteRegistrationInput); el test "con password" asienta accessToken pero no refreshToken.

# Batch endpoints App (conductor + flotilla) — 2026-06-22 (subagent-driven, sin commits)
Specs: docs/superpowers/specs/2026-06-22-endpoints-conductor-flotilla-design.md (+ 2026-06-22-bolsa-de-trabajo-design.md = DIFERIDO, solo spec)
Planes: rechazar-viajes (10 tasks) · asignacion-y-aceptar (4) · editar-perfil (3) · ganancias (4)
Restricción dura: NINGÚN comando git (no add/commit/push). Ejecución por feature (implementador + revisor). Secuencial: comparten la BD de test viajeseguro_test (los beforeEach truncan tablas) → no correr tests en paralelo.
Migraciones nuevas: 009_viaje_rechazos, 010_asignaciones_conductor_vehiculo, 011_conductor_sesiones.
- Rechazar viajes: COMPLETO. Implementador DONE (sin desviaciones). Revisor: Spec ✅, calidad Aprobada, sin Critical/Important (solo menores no-bloqueantes). 5/5 en rechazar.test.ts, suite 25 archivos/113 tests verde, typecheck limpio. Working tree. Migración 009 aplicada en test DB.
- Asignación + aceptar: COMPLETO. Implementador DONE (sin tocar tests existentes; wiring viajes→flotillas, sin ciclo). Revisor (opus): Spec ✅ (7/7 checks), calidad Aprobada, sin Critical/Important. 19 tests nuevos (6 repo asignación + 8 HTTP asignación + 5 aceptar autorización), suite 28 archivos/132 tests verde, typecheck limpio. Migración 010 aplicada. Working tree.
  - Minores para review final: (1) AsignacionPostgresRepository.asignar usa UPDATE-then-INSERT no transaccional en pool auto-commit; bajo concurrencia del mismo par revocado ambos podrían caer al INSERT pero el ON CONFLICT (partial unique) evita duplicado → no-op inocuo. Seguro para MVP. (2) asignacion.test.ts idempotencia HTTP no asienta el row count crudo (sí cubierto por el repo test).
- Editar perfil: COMPLETO. Implementador DONE (único new User() en UserBuilder.build; mapUserRow+completeRegistration heredan default false; sin tocar tests existentes). Revisor: Spec ✅ (7/7), calidad Aprobada, sin Critical/Important. 7/7 en editarPerfil.test.ts, suite 29 archivos/139 tests verde, typecheck limpio. Sin migración (reusa personas/usuarios/vehiculos). Working tree.
  - Minor para review final: schemas.ts idSexo/fechaNacimiento son .optional() sin .nullable() mientras la interfaz de actualizarPerfil acepta T|null (no se puede limpiar a null vía COALESCE; cosmético, sin bug). Documentado como límite MVP.
- Ganancias: COMPLETO. Implementador DONE (TZ America/Mexico_City con Intl longOffset DST-aware; horasEnLinea con GREATEST(...,0)+COALESCE(fin,now()); abrir idempotente partial-unique; setDisponibilidad preserva comportamiento). Revisor (opus): Spec ✅, calidad Aprobada, sin Critical/Important; confirmó explícitamente que el clamp negativo y la dirección de AT TIME ZONE están correctos. Fix aplicado: dedupe TZ (import desde horario.ts) + test de sesión fuera de rango. ganancias.test.ts 10/10, typecheck limpio. Migración 011 aplicada. Working tree.
  - Minores para review final: (1) hook de sesión en setDisponibilidad no transaccional con el upsert (MVP-ok); (2) hastaTs exclusivo en horasEnLinea vs ::date<=hasta inclusive en ganancias (ambos correctos, asimetría latente).

## Estado del batch: 4/4 features COMPLETAS (working tree, sin commitear).
Suite completa verde: 30 archivos / 149 tests. Typecheck limpio.
Review final del branch (opus): integración LIMPIA (sin clobbering en archivos compartidos viajes/dependencies.ts etc.), migraciones 009/010/011 contiguas e idempotentes, esPropietario y la autorización de aceptar usan la MISMA definición de "puede manejar" (single-sourced), sin regresiones (listarPendientesPorMunicipio sin callers stale). Los 4 menores = ACCEPTABLE-FOR-MVP. Veredicto: READY TO MERGE.
Pendiente del usuario: aplicar migraciones 009/010/011 en dev/prod; commit+push+redeploy; avisar a la App que aceptar ahora exige idVehiculo. Bolsa de trabajo = spec escrito, DIFERIDO. Método de cobro = diferido.

# Estimar viaje con geometría (Opción B) — 2026-06-22 (subagent-driven, sin commits)
Spec: docs/superpowers/specs/2026-06-22-estimar-viaje-geometria-design.md · Plan: docs/superpowers/plans/2026-06-22-estimar-viaje-geometria.md (5 tasks)
Alcance: POST /api/viajes/estimar (ruta GeoJSON siguiendo carreteras vía OSRM overview=full) + extender IRouteEstimator con geometria + fix 500 del :id (IdParamSchema en controllers de viaje) + crearViaje guarda distanciaKm. Sin migración.
- Estimar viaje geometría: COMPLETO. NOTA: la Task 1 (RutaGeoJSON + IRouteEstimator.geometria + ambos estimadores + sus tests) YA estaba en el working tree al inicio de la sesión (los 4 archivos M del git status inicial) — el implementador construyó el resto encima. Revisor: Spec ✅ (6/6), calidad Aprobada, sin Critical/Important (2 menores triviales). estimar 3/3 + idParam 2/2 + osrm 5/5 + haversine 1/1; suite 32 archivos/154 tests verde; typecheck limpio. Sin migración. Working tree.
  - Incluye fix del 500: los 7 controllers de viaje con :id ahora usan IdParamSchema.parse (:id no numérico → 400, antes 500). + crearViaje guarda distanciaKm numérico.

# Invitaciones de admins — 2026-06-22 (subagent-driven, sin commits)
Spec: docs/superpowers/specs/2026-06-22-invitaciones-admin-design.md (migración 012; POST /api/admin/invitaciones + GET + DELETE; POST /api/auth/invitaciones/aceptar; IInvitationSender Brevo+Mock; createAdmin sin persona; env ADMIN_PANEL_URL/BREVO_INVITE_TEMPLATE_ID/INVITE_TTL_DAYS).
- Invitaciones admin: COMPLETO. Plan 5 tasks. T1-T4 implementadas (un agente murió por error de conexión a mitad de T5 pero T1-T4 quedaron OK: typecheck limpio, migración 012 aplicada, repo+sender tests verdes). Un 2do agente completó T5 (test E2E invitaciones.test.ts 13/13, sin bugs de producción). Revisor (opus): Spec ✅, calidad/seguridad Aprobada, sin Critical/Important — confirmó token alta-entropía solo sha256 guardado, single-use, transacción atómica create-admin+marcar-aceptada, sin escalada de privilegios. Hueco: faltaba registro OpenAPI de los 4 endpoints → FIX aplicado (+ assert de password débil tensado). Suite final 35 archivos/175 tests verde, typecheck limpio. Migración 012. Working tree.
  - CONTRATO CAMBIADO (avisar a App): CorreoYaRegistradoError ahora emite code 'CORREO_YA_REGISTRADO' (antes 'CONFLICT') — afecta también POST /api/auth/register/start (mismo 409, distinto error.code). Spec lo pedía; clientes que ramifiquen por status no se afectan.
  - Pendiente del usuario: crear plantilla Brevo (BREVO_INVITE_TEMPLATE_ID, params ACCEPT_URL/EXPIRY_DAYS/INVITER_EMAIL) + setear ADMIN_PANEL_URL; panel: página /aceptar-invitacion?token= + UI invitar/listar/revocar; aplicar migración 012 en dev/prod.

# Cifrado y privacidad — Fases 1 y 2 (motor puro) — 2026-06-25 (subagent en background, sin commits)
Spec: docs/superpowers/specs/2026-06-25-cifrado-privacidad-design.md · Clasificación: docs/seguridad/clasificacion-datos-privacidad.md · Plan: docs/superpowers/plans/2026-06-25-cifrado-privacidad-fases-1-2.md (4 tasks)
Alcance: motor de cifrado PURO (sin BD ni repos). ICipher + AesGcmCipher (AES-256-GCM texto/bytes + blind index HMAC) + buildCipher/env CIPHER_KEY+CIPHER_INDEX_KEY (fail-closed) + sensitiveFields + CipherPipeline (filtros cifrar/blind-index/descifrar/anonimizar) + CipherCodec.
- Fases 1-2: DONE (un subagente sonnet en background ejecutó las 4 tasks). 4 archivos de test / 23 tests verdes, typecheck limpio, sin desviaciones del plan. Working tree (sin commitear).
  - Archivos: src/core/crypto/{ICipher,AesGcmCipher,cipher,sensitiveFields,CipherPipeline,CipherCodec}.ts + tests/core/crypto/*.test.ts; modificados src/core/errors.ts (CipherConfigError/CipherError) y src/core/env.ts (CIPHER_KEY/CIPHER_INDEX_KEY opcionales).
  - Realineación de capas HECHA (feedback del usuario "no perder arquitectura"): puerto ICipher + lógica pura (CipherPipeline/CipherCodec/sensitiveFields) en src/core/crypto/; adapter AesGcmCipher + wiring cipher.ts movidos a src/infrastructure/crypto/ (precedente IDocumentStorage). Tests movidos a tests/infrastructure/crypto/. Verde: infra 13 + core 10 = 23 tests, typecheck limpio. NOTA: vitest falla con "reading 'config'" si se le pasan 2 rutas posicionales a la vez → correr una carpeta por invocación. Falta review de tarea formal del cipher.

# Zonas Calientes — microservicio ML (REPO APARTE: ZonasCalientes-ML/) — 2026-06-25 (subagent-driven, sin commits)
Spec: docs/superpowers/specs/2026-06-25-zonas-calientes-microservicio-design.md · Memoria: [[viajeseguro-zonas-calientes-ml]] + [[profesor-ml-estructura-esperada]]
Planes: análisis-datos (5 tasks) → microservicio (7 tasks). Repo Python aparte (carpeta hermana ya creada con app/training/tests/notebooks/docs/bruno + ejemplos/ del profe). El usuario hace git init; nosotros creamos archivos. Diagrama arquitectura en FigJam (board Jala).
Constraints duras: SOLO no supervisado (DBSCAN haversine + NearestNeighbors/LOF; NO KNeighborsClassifier); NUNCA jalar de BD transaccional; modelo SIEMPRE offline (MLflow registry); FastAPI modo inferencia; bucketing (municipio, dia_tipo, hora EXACTA). Alineado al ejemplo del profe (generador+EDA tabular/visual+clustering+documentation, ground-truth, nulos/anomalías).
Entregables académicos: repo, PDF LaTeX, notebook evidencia, diagrama arq (pdf/png/svg), API docs (pdf/md), evidencia entrenamiento, colección Bruno/Postman. (Video lo hace el usuario.)
- Análisis Task 1 (config YAML + documentation 01/03 + requirements-analisis.txt): COMPLETO. YAML parsea (13 claves), docs 01/03 creados, sin desviaciones. Working tree.
- Análisis Task 2 (generate_synthetic.py + tests): COMPLETO. TDD RED→GREEN, 4/4 tests. Generó 48383 filas, 964 anomalías, nulos 1451, dups 479; validar_dataset detecta coords_fuera_bbox 486 + hora_invalida 478. Datasets profesor/estudiante + manifest en documentation/. Desviación mínima: conftest.py (sys.path) para layout plano. Working tree.
- Análisis Task 3 (eda_tabular.ipynb): COMPLETO. nbconvert --execute limpio, 35 celdas/14 secciones. Hallazgos: horas pico 17-19 y 7-8, corr distancia-costo 0.92, 964 anomalías = flag_anomalia, ruido 7.9%, municipios balanceados. Working tree.
- Análisis Task 4 (eda_visual.ipynb): COMPLETO. nbconvert limpio, 13 figuras PNG en figures/, scatter geo muestra las zonas plantadas. Working tree.
- REDISEÑO DOS NIVELES (decisión final del usuario): plan reescrito (docs/superpowers/plans/2026-06-25-zonas-calientes-analisis-datos.md). Nivel A solicitudes crudo + Nivel B demanda_agregada (celda×franja×municipio, demand_density/supply_demand_ratio). Clustering sobre Nivel B (+ DBSCAN puntos como baseline). Features achievable only (sin surge/weather/event_flag/wait_time_min). Por municipio. Validación ARI/NMI vs is_hot_true/zone_id_true. CSV viejos (nivel único) eliminados. Regenerando Tasks 1-5 con el nuevo esquema.
- Análisis Tasks 1+2 (config+docs dos niveles + generate_synthetic.py A+B + tests): COMPLETO (regenerado). 48783 solicitudes (A), 5451 celdas (B), 1089 hot. 4/4 tests, sin desviaciones.
- AJUSTE PRECIO (decisión usuario): costo_mxn NO dinámico → tarifa FIJA por zona de destino (espeja TarifaPorZona real) + columna zona_destino. COMPLETO: costo_mxn ∈ {12,14,15,16,18,20}, 4/4 tests, regenerado (48783 solicitudes / 5357 celdas / 1096 hot). Working tree.
- Análisis Task 3 (eda_tabular.ipynb sobre Nivel A + Nivel B): COMPLETO. nbconvert limpio, 43 celdas/14 secciones. VALIDA el diseño: celdas hot demand_density 432 vs 13; supply_demand_ratio 0.29 vs 3.67 (separación clara); corr density↔ratio -0.34. Working tree.
- Análisis Task 4 (eda_visual.ipynb sobre Nivel A + Nivel B): COMPLETO. nbconvert limpio, 18 figuras PNG. Mapa de calor B: density mediana hot 211 vs 11 (19×); scatter density-vs-ratio = dos nubes separadas (hot ratio 0.18 vs 3.5). 3 PNGs viejas eliminadas. Working tree.
- Análisis Task 5 (clustering.ipynb sobre Nivel B + notas): COMPLETO (opus). nbconvert limpio (15 celdas), pytest 4/4, 6 figuras clu_*. DBSCAN-B por municipio: muni1 ARI 0.19/precision hot 0.73, muni2 ARI 0.15/precision 0.33. Baseline puntos (A haversine) ARI=1.0. LOF=ruido DBSCAN 100%. 7 zonas extraídas. notas-clustering.md a fondo.
- HALLAZGO CLAVE (informa el microservicio): en UN bucket las celdas calientes son ANOMALÍAS (pocas/extremas), no clusters densos → DBSCAN sobre todo el grid B engancha el fondo de baja demanda. Fix: seleccionar celdas de alta demanda/bajo ratio y agruparlas espacialmente (DBSCAN haversine), o clusterizar orígenes A + scoring con B. DECISIÓN PENDIENTE del usuario antes de construir el microservicio.
## FASE DE ANÁLISIS DE DATOS: COMPLETA (5/5 tasks). Entregables en ZonasCalientes-ML: generador 2 niveles + 3 notebooks + documentation 01/03/04 + figures + datasets. Working tree (usuario hizo el push al repo).

# Zonas Calientes — MICROSERVICIO FastAPI (ZonasCalientes-ML/) — 2026-06-26 (subagent-driven, sin commits)
Spec/Plan REESCRITOS al modelo final: por (municipio, dia_tipo, hora) → FILTRAR celdas alta demanda del Nivel B (demand_density>=50 Y supply_demand_ratio<=1.0) → DBSCAN haversine (eps_m=450, min_samples=1) sobre centroides → rankear por densidad. Reutiliza el Nivel B ya generado (documentation/demanda_agregada_profesor.csv). Entorno: intérprete `py` (NO python), deps globales (mlflow 2.17.2/sqlmodel/pydantic-settings instaladas esta sesión; pip bajó protobuf a 5.x → choca con tensorflow global del usuario, conviene venv).
- Tasks 1-4 (scaffold+health/swagger, BD inferencias SQLModel+repo, cargar_demanda Nivel B, clusterizar_demanda+ZonaCaliente+train.py+MLflow): COMPLETO (implementador sonnet). 14/14 tests verdes (confirmado por mí). Smoke train con datos reales: 96 buckets/240 zonas, precision_hot_filtro=1.0, recall=0.969, modelo `zonas-calientes` v1 en mlruns/. Review propio limpio (no supervisado OK: is_hot_true solo en métricas; centroide ponderado; intensidad normalizada). Working tree.
  - Minor (plan-mandated): datetime.utcnow() deprecado en models.py (3.12). MLflow warning sin firma de modelo. No bloqueante → review final.
- Tasks 5-7 (inferencia POST + historial GET + despliegue Docker/compose): COMPLETO (implementador B sonnet). 18/18 tests verdes (confirmado por mí). Desviaciones correctas: StaticPool en tests SQLite en memoria, session.py importa models para registrar metadata, Task 6 junto con Task 5.
- Review final (opus, independiente): SIN Critical, sin fuga supervisada, sin violaciones de capa, sin bugs de correctitud (verificó el indexado posicional de pandas). 2 Important (contrato/Swagger) + minors → TODOS ARREGLADOS por mí:
  - I-1: modelo_version reportaba "latest" → ahora ModeloZonasMlflow._resolver_version() resuelve la versión numérica (search_model_versions). E2E confirma version="1".
  - I-2: GET /inferencias sin response_model → nuevo schema InferenciaHistorialOut + response_model=list[...] (Swagger documenta el historial).
  - Minors: datetime.now(timezone.utc) en models.py; Dockerfile python:3.12-slim; limit/offset con Query(ge/le); README con sintaxis PowerShell.
- VERIFICACIÓN E2E REAL (no solo ModeloFake): app con lifespan carga el modelo MLflow registrado (/health modelo_cargado=true, version=1); POST 1/jue/18 → 3 zonas (top demand_density=1036 en el centro real 16.7516,-93.1161, 653 req, 7 celdas); persistencia + historial OK. Carga desde model registry con file store (./mlruns) funciona.
## MICROSERVICIO FASTAPI: COMPLETO (7/7 tasks, 18 tests, E2E verificado). Entorno: intérprete `py`; deps globales (conviene venv por el downgrade de protobuf). Working tree (sin commitear; el usuario commitea/pushea).
  - VALIDACIÓN DEL MODELO (evidencia académica): COMPLETA. training/validar.py (métricas vs ground-truth + log a MLflow run "validacion") + notebooks/validacion_modelo.ipynb (ejecutado, 10 celdas, 4 figuras val_*.png). Resultados sobre datos reales: recuperación espacial mediana 15.7 m al centro real (100% < 300 m), ARI=NMI=1.0 (incl. buckets multi-zona), precision_hot=1.0/recall=0.969, coherencia temporal sigue el patrón plantado, generalización con semilla 123 (datos nuevos) mediana 16.5 m / 100% < 300 m. Presentado honestamente (sintético bien separado por diseño). is_hot_true/zone_id_true SOLO para medir.
  - ENTREGABLES + DEPLOY (2026-06-26): COMPLETO lo siguiente. Colección Insomnia nativa (insomnia/zonas-calientes.insomnia.json, 8 requests) + openapi.json exportado (documentation/openapi.json). API docs en markdown (documentation/api-zonas-calientes.md). Deploy prep: docker-compose.yml endurecido (mlflow --serve-artifacts, healthchecks db+api, restart, depends_on condition, MODELO_NOMBRE/STAGE env) + .dockerignore + runbook documentation/DEPLOY.md (pasos Coolify: deploy → entrenar una vez en el contenedor `python -m training.train` → reiniciar api → /health modelo_cargado=true). `docker compose config` válido. NOTA deploy: en contenedor el intérprete es `python` (no `py`); el protobuf/venv local no aplica en Docker (aislado).
  - VERIFICACIÓN LOCAL CON DOCKER: PASÓ (2026-06-27, Docker 29.2.1). docker compose up --build → 3 servicios healthy (api/db Postgres/mlflow). /health pre-train modelo_cargado=false. Entrenó EN el contenedor (`python -m training.train`) → registró zonas-calientes v1 en el MLflow del stack. Tras `docker compose restart api`: /health modelo_cargado=true version=1 (la API baja el modelo del registry vía --serve-artifacts, OK). POST /inferencias → 3 zonas (top density 1036 en el centro real), dia_semana=9 → 422, historial persistido en Postgres (1 registro). Validación en contenedor: mismos números (dist 15.7m, ARI 1.0, precision 1.0/recall 0.969). Warning GIT_PYTHON inofensivo (slim sin git). Stack quedó CORRIENDO en local (api :8000/docs, mlflow :5000). El MISMO compose va a Coolify.
  - AUTH RS256 (DIFERIDA — el deploy MVP va SIN auth, decisión del usuario): spec + plan ESCRITOS. Spec docs/superpowers/specs/2026-06-27-auth-jwt-rs256-zonas-calientes-design.md, plan docs/superpowers/plans/2026-06-27-auth-jwt-rs256-zonas-calientes.md (3 tasks). Topología: app llama DIRECTO al microservicio con su access token; RS256 (backend firma con privada, micro verifica con pública → no comparte secreto, no puede falsificar). Solo el access token migra a RS256 (refresh/registration siguen HS256). Micro: pyjwt + requiere_conductor (rol conductor/propietario, fail-closed sin JWT_PUBLIC_KEY, /health abierto). Implementar DESPUÉS del deploy.
  - DEPLOY EN COOLIFY: HECHO (2026-06-27). EN VIVO en https://zonas.codigoverse.space (Swagger /docs). Recurso Docker Compose. Fixes aplicados: `expose` en vez de `ports` (el host 8000 lo ocupa el propio Coolify → "port is already allocated"); "Connect To Predefined Network" ON (si no, Traefik = "no available server"); dominio zonas.codigoverse.space al servicio api. Modelo entrenado en el contenedor (`python -m training.train`) + Redeploy → modelo_cargado=true v1. Primera inferencia real OK (muni1/mié/12 → 3 zonas = los 3 centros reales del muni 1, rankeadas por demanda). Lecciones reusables en [[viajeseguro-coolify-deploy]].
  - ENTREGABLES DOC (2026-06-27): HECHO los 3. (1) Diagrama de arquitectura con Graphviz → documentation/arquitectura.{dot,svg,png,pdf}. (2) API docs PDF (documentation/api-zonas-calientes.pdf) vía markdown+Edge headless (weasyprint NO sirve en Windows: falta libgobject/GTK). (3) Reporte LaTeX reporte/reporte.{tex,pdf} (6 págs, MiKTeX/latexmk; embebe el diagrama + las 5 figuras de validación + tabla de métricas; necesitó \usepackage{amsmath} por \text/\tfrac). Herramientas verificadas: dot (Graphviz), pdflatex/xelatex/latexmk (MiKTeX en %LOCALAPPDATA%), Edge headless para HTML→PDF.
  - PENDIENTE: implementar el plan de auth RS256 (diferido); cablear el cipher en los repos (Fase 3+, el usuario lo pidió "después"); video (lo hace el usuario).

# Endurecer Auth (BackendViajeseguro) — 2026-06-27 (brainstorming → spec → plan)
Spec: docs/superpowers/specs/2026-06-27-auth-hardening-otp-ratelimit-design.md · Plan: docs/superpowers/plans/2026-06-27-auth-hardening-otp-ratelimit.md (5 tasks). Es el ESFUERZO A de 2 (B = cablear el cipher para PII, pendiente, otro spec).
Hallazgos del mapeo: hoy NO hay rate-limit en /api/auth/*; OTP = 4 díg con Math.random (predecible); cipher 100% construido (src/core/crypto + src/infrastructure/crypto) pero 0% cableado (PII en texto plano). Tests: vitest ^4 + supertest ^7; express-rate-limit/ioredis NO instalados aún.
Diseño aprobado por el usuario: OTP 4→6 díg + crypto.randomInt; rate-limiting con express-rate-limit + **Redis** (decisión del usuario; store inyectable, MemoryStore en dev/test, skip en NODE_ENV=test, fail-open, trust proxy=1, 429+Retry-After, llave envío = IP+sha256(destino)). Límites por env con defaults.
- Tasks 1-4: COMPLETO (implementador sonnet). typecheck PASS, suite 212/212 (44 archivos) confirmado por mí. Verificado núcleo (rateLimiters.ts, redis.ts) + grep sin \d{4} de OTP residual. Desviaciones correctas: import named `{ Redis }` (NodeNext), fix TS en storePorDefecto, **schemas.ts /^\d{4}$/→/^\d{6}$/** (el OTP se valida también en el borde; el plan no lo tenía), fixture flow.test '0000'→'000000'. Working tree (sin commitear).
- Review final (opus): SIN Critical. 1 Important (I-1: bypass del rate-limit por-destino — llaveDestino usaba el correo crudo mientras el blind index normaliza .trim().toLowerCase(), así variantes de mayúsculas/espacios evadían el límite anti-spam) → ARREGLADO (llaveDestino normaliza igual + exportada) + test anti-bypass. 2 minors aceptados (M-1 IPv6 req.ip crudo = igual que el default de la lib, bajo riesgo tras Traefik; M-2 validate:false defendible). Confirmado correcto: orden de middleware (express.json antes), skip-en-test seguro, fail-open, trust proxy=1, OTP uniforme. typecheck OK, suite 213/213.
## ESFUERZO A (auth hardening): COMPLETO (Tasks 1-4 + review). Working tree (sin commitear).
- PENDIENTE del usuario: Task 5 (contenedor Redis en Coolify + REDIS_URL); commit/push; avisar a las apps que el OTP ahora es de 6 díg. Luego ESFUERZO B (cablear el cipher para PII).

# Parte 1 — Multi-rol backend fase 1 (2026-07-07, subagent-driven, CON commits en feature/multirol-bolsa-trabajo)
Plan: docs/superpowers/plans/2026-07-07-parte1-multirol-backend.md · Spec: docs/superpowers/specs/2026-07-07-identidad-multirol-bolsa-trabajo-design.md · BASE T1: 7e25d81
Esta corrida SÍ commitea (decisión del usuario: ramas feature en los 3 repos). Regla nueva: OpenAPI siempre al día.
NOTA para Partes 3-4: YA EXISTEN 010_asignaciones_conductor_vehiculo + 018_unique_conductor_activo + spec bolsa 2026-06-22 (DIFERIDO) → reconciliar el spec nuevo contra eso ANTES de planear.
En paralelo (otros repos, rama homónima): prep de apps conductor/pasajero = parseo tolerante roles[].
- Prep apps COMPLETO (ambas, en paralelo con T1): conductor cf39990 (3/3 tests; su mapper NO tiene toJson — skip correcto) · pasajero eedc75d (3/3 + suite 17/17; user_roles.dart = solo const defaultRole, sin impacto). Ambas parsean roles[] con fallback a rol + helpers es*.
- Nota pasajero: flutter test requiere .env local (gitignoreado); dev/CI nuevos deben copiarlo de .env.example (preexistente).
- T1 implementador: DONE, commit 23a420f (020_usuario_roles.sql). Verificado sin_roles=0 (local solo tiene 1 admin → backfill-2 sin dataset mixto local). ENTORNO REAL: Postgres Homebrew rol viajeseguro / db viajeseguro_dev (el .env trae postgres/viajeseguro_local — NO tocado); docker NO instalado, compose sin servicio db → futuros dispatches no deben asumir docker. Pendiente: review de tarea.
- Task 1: COMPLETO (commit 23a420f, review limpio: Spec ✅, calidad Aprobada, 0 Critical/Important). Minors para review final: (m1) backfill-2 'pasajero' universal sin evidencia de ejecución con dataset mixto — ejercitarlo en T7 al crear usuarios de prueba no-admin; (m2) .env local desalineado (preexistente, no tocar).
- Task 2: COMPLETO (commit a64a843, review: Spec ✅, Aprobada, 0 Critical). Adjudicado por controller: ventana de typecheck roto (sessionTokens.ts) entre T2→T5 es secuenciación del plan, contenida en la rama (no se mergea hasta typecheck verde en T7/review final). Minors para review final: (m3) test no asserta type==='access' ni rechazo de tokens no-access (preexistente en verify()); (m4) rama legacy roles?:undefined solo verificada por tipos, un test de 1 línea la haría self-verifying.
- Task 3: COMPLETO (commit 9e3ca98, review: Spec ✅, Aprobada, 0 Critical/Important). requireRole por intersección + fallback [rol]; 7 call sites de viajesRoutes confirmados type-safe. Minor observación: sin test dedicado del path 401 (preexistente, el brief no lo pedía).
- HALLAZGO REPO: la suite histórica (~213 tests de sesiones previas) NO existe en el árbol actual — solo hay 2 archivos .test.ts (los nuestros, jwt+middleware). Las sesiones viejas dejaban tests en working tree y no sobrevivieron a los commits del usuario. Implicación: "pnpm test verde" es evidencia débil; T7 smoke E2E es la verificación real. Avisar al usuario al cierre de la parte 1.
- Task 4: COMPLETO (commit ee6f462, review: Spec ✅, Aprobada, 0 Critical/Important). getRoles/addRol con patrón real de la clase; INSERT inicial dentro de la transacción con client correcto; columnas verificadas vs 020. Minors para review final: (m5) IUserRepository importa type Rol desde core/jwt (plan-mandated, cosmético — candidato a mover Rol a dominio); (m6) getRoles sin validación runtime del enum (patrón preexistente, cubierto por smoke T7).
- Task 5: COMPLETO (commit 45d0a51, review: Spec ✅, Aprobada, 0 Critical/Important). TYPECHECK DEL REPO LIMPIO desde aquí. getRoles tras los guards en los 3 flujos; sin callers huérfanos. Minor para review final: (m7) usuario con 0 roles → token 'pasajero' silencioso vía rolPrincipal([]) — follow-up: log/alerta en emisión si roles.length===0 (enmascara problemas de integridad).
- Task 6: COMPLETO (commit 42139ab, review: Spec ✅, Aprobada, 0 Critical/Important). /me con roles[] (extend de PublicUserSchema, mismo ref RolSchema en OpenAPI — verificado generando el doc real). Curl smoke diferido a T7 (env local sin CIPHER keys). BASE T7: 42139ab.
- Task 7: COMPLETO (sin commits — verificación pura, 0 fixes necesarios). E2E real: login password ✓ token roles[] ✓ /me ✓ gate conductor con rol principal propietario ✓ (200 en asignados+pendientes) refresh ✓ backfill-2 con evidencia de ejecución ✓ (cierra m1) audit apps ✓. Nota operativa: limpiar datos de prueba por id_usuario, no por columna plaintext (cipher). PENDIENTE: review final de rama (opus).
- Review final de rama (opus): LISTO CON UN FIX. Único gap de integración: socketServer.ts:38/42/64 (6º consumidor de tokens) lee user.rol principal en vez de roles[] → conductor multi-rol pasaría HTTP pero NO podría ponerse online por socket (latente hoy, se activa con Parte 2). Fixer despachado (roles ?? [rol] + .includes, patrón de requireRole) + comentario en getMeController:47 (rol legacy vs principal derivado — al DROP COLUMN en fase 2, /me debe servir rolPrincipal(roles)).
- TRIAGE minors (veredicto reviewer final): m1 CERRADO (evidencia T7) · m4 SATISFECHO (test 3 del middleware ya cubre legacy) · m2, m6 ACEPTADOS · m3, m5, m7, obs-T3 → FOLLOW-UP TICKETS (no bloquean).
- REGLA PARA PARTE 2: (1) socket path E2E del conductor multi-rol es parte del alcance; (2) /me con rolPrincipal(roles) cuando caiga usuarios.rol; (3) el fix de socket es prerequisito duro.
- Fix review final: COMPLETO (commit 0234f34). Reviewer re-verificó git show: 3 gates de socket con roles.includes + fallback (espejo de authMiddleware:31, roles fijo por conexión = correcto), comentario en getMeController, typecheck 0 / 5 tests verdes. VEREDICTO: CONFIRMADO — rama lista para merge.
## PARTE 1: COMPLETA (8 commits de código: 23a420f→0234f34 + 4 docs). DECISIÓN USUARIO: NO mergear aún — todas las partes se acumulan en feature/multirol-bolsa-trabajo (3 repos) y el merge es único al final tras comprobar todo. Sigue: Parte 2 (registro propietario+pasajero + onboarding conductor progresivo).

# Parte 2 — Registro propietario+pasajero y onboarding progresivo (2026-07-07, subagent-driven, CON commits)
Plan: docs/superpowers/plans/2026-07-07-parte2-registro-onboarding.md (7 tasks: B1 registro multi-rol, B2 gate relajado, B3 addRol al aprobar, A4 app registro, A5 gate invertido+CTA, B6 openapi, E2E7 con socket multi-rol).
Exploración previa: backend (registro descarta rol en start, lo firma en verify:6-17, completa en completeRegistration:25-44; gate duro conductorRoutes:8; punto de otorgamiento reviewDocumento:31-37; flotillas tiene asegurarExiste). App (flujo registro→license entre municipio y photo; gate splash:46; rol hardcoded 127/151; vehicles ya es tab).
Reconciliación P3 anotada: migración 010 = M:N asignaciones SIN vehículo-activo; 018 es de viajes; aceptarViaje exige idVehiculo del cliente. El spec de bolsa 2026-06-22 NO existe como archivo (solo se mencionó en ledger; el diseño vigente es el spec 2026-07-07).
- P2-A4 implementador: DONE (conductor f5563d1). completeRegistration movida a register_municipio (patrón replicado); _normalizeDate eliminado (dead code); license screen intacta pero INALCANZABLE y aún llama completeRegistration → ADAPTARLA antes de recablear /license al onboarding (futuro). Pendiente review.
- P2-A4: COMPLETO CON ADJUDICACIÓN (conductor f5563d1). Review: items 1,2,4 ✅ limpios; "Critical" = gate de documentos en driver_home:37-44 rebota al registrante nuevo → es EXACTAMENTE el scope de A5 (ventana temporal intra-rama, mismo patrón que T2→T5 de P1). ADJUDICADO: A5 es el fix, despachado inmediatamente después; E2E T7 verifica el flujo end-to-end antes de cerrar P2. Minors: rename _irADocumentos (se folda en A5) · license_screen nunca llamó guardarLicencia (anotado: el reuso futuro para onboarding requiere recablearla, no es template funcional).
- P2-B1: COMPLETO (commit b87bffd, review: Spec ✅, Aprobada, 0 Critical/Important). Mapeo 3 líneas legible; loop roles en misma tx; asegurarExiste no-fatal con console.error; deviations justificadas (.describe en schemas.ts con rolRegistro compartido; export const propietarios). Minor: console.error sin campos estructurados (no hay logger en el repo). Checks: 1 solo caller de createUserWithPersona; sin ciclo auth→flotillas; createAdmin/aceptarInvitacion intactos.
- ⚠️ COORDINACIÓN: el usuario está editando EN VIVO home_viewmodel.dart y driver_home_screen.dart (conductor) — mismos archivos que A5. Instrucción enviada a A5: preservar ediciones humanas, commitear SOLO splash/profile/photo, dejar los 2 compartidos sin commitear y reportar la atribución de hunks. El commit compartido se coordina con el usuario después.
- P2-A5 implementador: DONE (conductor 9cc5571, 6 archivos +47/-83). CORRECCIÓN del controller: la alarma de "ediciones humanas en vivo" fue FALSO POSITIVO — _StatsRow/_onlineSince/onRideAccepted ya estaban en la BASE commiteada (commits del usuario de la semana previa a crear la rama); el agente verificó pre-touch y decidió bien al commitear sus 6 archivos. LECCIÓN: system-reminders de archivos modificados pueden reflejar drift de base no releída, no WIP concurrente — verificar con git log/diff ANTES de coordinar. Hallazgos del agente para su review: (1) login_screen.dart tenía el MISMO gate → fix root-cause incluido; (2) resolveDocumentsRoute quedó con 0 callers (dead code, decisión pendiente); (3) REGRESIÓN potencial: splash perdió el path UnauthorizedException→login (iba montado en la llamada de red del gate) — sesión muerta ya no se detecta al arranque.
- P2-B2: implementador DONE (986374c, 4/4 tests, typecheck limpio). Pendiente review.
- P2-B2: COMPLETO (commit 986374c, review: Spec ✅, Aprobada, 0 issues). Gate único del router relajado a conductor|propietario, sin sub-gates por ruta.
- P2-A5 review: Needs fixes → 1 Important REAL: _LandscapeBottomBar sin gate esConductor (Switch visible a no-conductores en landscape; solo portrait se protegió). Fixer despachado. Important #2 DOWNGRADED por el propio reviewer: la regresión del splash (sesión muerta) SE AUTO-SANA (_killSession→goToLogin por router raíz, verificado cableado) — costo = un flash del home + 1 round-trip. FOLLOW-UP (triage en review final P2): splash podría hacer un getMe barato antes de rutear. Confirmado también: (a) login_screen fix justificado; (b) resolveDocumentsRoute 0 callers (decisión de borrado → review final); getDocumentos() en loadData ya no gatea nada (candidato a trim futuro).
- P2-A5: COMPLETO (commits 9cc5571 + fix a81145d; re-review: CONFIRMADO, aprobada). Gate esConductor en ambas orientaciones; duplicación del banner adjudicada aceptable (extraer si aparece 3er caller o contenido dinámico). Follow-ups ya anotados arriba (splash preflight, resolveDocumentsRoute muerto, trim getDocumentos).
- P2-B3: COMPLETO (commit 99c58c2, review: Aprobada, 0 Critical/Important). addRol primero y sin try/catch (fallo→review reintentable, idempotente); test de orden con secuencia real; desviación de wiring JUSTIFICADA (users deps no exporta instancia; auth self-instancia; clase stateless sobre pool). Nota: B3 ya extendió el summary OpenAPI del endpoint de revisión → B6 solo cubre summaries de onboarding/documentos si falta.
- P2-B6: implementador DONE (ea51b66, 7 summaries con "rol conductor o propietario", typecheck limpio). Review haiku en curso. E2E (task 7 P2) despachado en paralelo.
- P2-B6: COMPLETO (ea51b66, review Aprobada). Falta solo E2E (task 7) + review final de rama P2.
- P2-E2E (task 7): COMPLETO, DONE sin fixes. Registro HTTP propietario ✓ (2 roles + propietarios en BD) · onboarding 200 como propietario, conductores auto-creado ✓ · 3 docs aprobados → usuario_roles ganó conductor ✓ · refresh con 3 roles ✓ · SOCKET E2E REAL ack {ok:true,idMunicipio:1} ✓ (regla P1 cumplida) · compat app vieja rol=conductor → propietario+pasajero ✓ · cleanup 8 tablas + storage ✓. Reporte: parte2-task-7-report.md. PENDIENTE: review final de rama P2 (opus, 2 repos).
## PARTE 2: COMPLETA. Review final (opus, 2 repos): READY, cero fixes de código. Triage: f1 TICKET (splash getMe preflight, UX) · f2+f4 TICKET BUNDLE para P3 (dead-code: resolveDocumentsRoute + RegisterLicenseScreen/ruta + su completeRegistration stale) · f3 CERRADO (premisa obsoleta: loadData ya no llama getDocumentos) · f5 ACEPTADO (duplicación banner). Polish opcional: gate del Switch por esConductor (flash cosmético). NOTA ESTRUCTURAL para P3+: el response de auth lleva solo `rol` (PublicUser); `roles[]` SOLO en JWT + /me — pantallas que necesiten multi-rol post-login deben leer /me.
# Parte 3 — Asignación conductor→vehículo (vehículo activo). DECISIÓN DE RECONCILIACIÓN (ponytail, reuso): EXTENDER la tabla 010 asignaciones_conductor_vehiculo (ya tiene activo bool + unique parcial por par) en vez de crear asignaciones_vehiculo del spec §3.2: agregar origen ('propia'|'bolsa', default propia) + unique parcial POR CONDUCTOR where activo (un vehículo activo por conductor) + autoasignación primer vehículo + backend deriva el activo en online/aceptar (idVehiculo del cliente pasa a opcional/compat). Exploración adicional despachada (flotillas/disponibilidad/app vehicle).
DECISIÓN P3 CORREGIDA tras exploración: 010.activo = AUTORIZACIÓN (no selección) → la selección activa vive en conductores.id_vehiculo_activo (1 columna, migración 021). origen propia|bolsa DIFERIDO a P4. Plan: docs/superpowers/plans/2026-07-07-parte3-vehiculo-activo.md (7 tasks: B1 migración+hook registrar, B2 hook al ganar rol, B3 PATCH activo+flag lista, B4 aceptar deriva activo, A5 app selección, A6 tickets P2, E2E7). getMiVehiculo determinista → acceptRide de la app NO se toca.
- P3-A6 implementador: DONE (conductor e73d3a6, +24/-309). f1 preflight getMe en splash; f2+f4 borrados (greps 0); polish = solo verificación (ambos Switch dentro de ramas esConductor). Nota: guardarLicencia vive en auth_repository/api (sin callers), no en el VM — conservado sin tocar. Pendiente review.
- P3-A6: COMPLETO (e73d3a6, review: Spec ✅, Aprobada, solo 1 nit de comentario). f1: catch UnauthorizedException armónico con _killSession Y load-bearing en el sub-caso 401-tras-refresh-exitoso. f2+f4: greps 0 en lib/ y test/, constante e import limpiados juntos. Campos de licencia preservados con el comentario exacto en 4 sitios.
- P3-T1 implementador: DONE (6f5fcf2, 1104s — el usuario lo creyó congelado, era paso lento). Migración 021 idempotente; hook con findById+getVehiculoActivo compuestos (sin método nuevo). Suite 6 archivos/14 tests. Pendiente review.
- P3-T1: COMPLETO (6f5fcf2, review: Spec ✅, Aprobada; reviewer re-corrió tsc+vitest él mismo). Verificado: findById de conductores (null=sin fila; identidad 1:1 con propietarios vía id_usuario); timing correcto (propietario sin rol → no-op, difiere a T2); sin ciclo de imports. Minors: conteo "6 archivos" en prosa (son 5); console.error sin ids en payload (convención del repo).
- P3-T2 implementador: DONE (cc596ff, 16 tests). Reusó IVehiculoRepository.listarPorPropietario; hook tras registrarCambioEstatus, no-fatal; min-id con reduce (test [9,4]→4). Pendiente review.
- P3-T2: COMPLETO (cc596ff, review: Spec ✅, Aprobada; reviewer re-corrió vitest+tsc). listarPorPropietario = strict ownership; guard evita clobber en re-aprobaciones; FK SET NULL → re-asignación auto-sanadora. Minor: comentario dice "más antiguo" (es min id, coinciden por ids secuenciales).
- P3-T3 implementador: DONE (d544db8, 19 tests). PATH REAL: PATCH /api/flotillas/vehiculos/activo (antes de /vehiculos/:id por shadowing). BONUS: bug preexistente zod-to-openapi (.partial().extend() sobre schema nombrado dropea campos — origen ya estaba roto en doc) arreglado con VehiculoResumenSchema propio. AUDITORÍA PENDIENTE: ¿el patrón .partial().extend() roto se repite en otros módulos? (candidato a tarea aparte). Pendiente review.
- DECISIÓN DE PRODUCTO (usuario, 2026-07-07): cuenta solo-pasajero en app conductor → UPGRADE ofrecido (no bloquear, no dejar 403s). Spec §5.2.1 nuevo: POST /api/flotillas/propietarios/activar (idempotente, cualquier rol autenticado) + pantalla de upgrade en app + refresh de tokens post-upgrade. ALCANCE DE PARTE 5 (+1 tarea backend, +1 app).
- P3-T3: COMPLETO (d544db8, review: Spec ✅, Aprobada; reviewer reprodujo el bug OpenAPI con la lib real — el fix VehiculoResumenSchema documentaba ANTES campos falsos y contaminaba VehiculoDetalle; ahora exacto). PATCH /api/flotillas/vehiculos/activo registrado antes de :id; 1 llamada getVehiculoActivo asertada con toHaveBeenCalledTimes(1). Minor: claim de conteo de tests ambiguo (6/19 es la suite completa, el diff agrega 1 archivo/3 tests).
- INSTRUCCIÓN DEL USUARIO (2026-07-07): al TERMINAR la Parte 6 (limpieza backend: drop usuarios.rol) y ANTES de la pasada de minimalismo en la app → escribir un HANDOFF COMPLETO a disco en .superpowers/sdd/handoff-contexto.md (estado de las 6 partes, decisiones, follow-ups vivos, objetivo del rediseño minimalista y pantallas candidatas, env local, rutas de specs/planes/reportes, cómo reanudar). El usuario hará /compact justo después; la sesión reanudada arranca leyendo ese handoff + este ledger.
- P3-T4 implementador: DONE (38c9880, 22 tests; RED verificado via stash). SinVehiculoActivoError(400, SIN_VEHICULO_ACTIVO) nuevo siguiendo patrón por-condición; wiring Pick<IConductorRepository,'getVehiculoActivo'>. CONCERN para review: el guard de sin-activo corre ANTES de resolver el viaje → conductor sin activo + viaje inexistente recibe SinVehiculoActivoError en vez de ViajeNoEncontradoError (orden de errores cambiado en ese edge). Pendiente review.
- DBML del esquema final commiteado (docs/superpowers/specs/jala-esquema-final.dbml) para dbdiagram.io.
- ⚠️ REGLA REAFIRMADA POR EL USUARIO (2026-07-07): NO commits ni push por agentes — él commitea. Los commits hechos hasta ahora son SOLO LOCALES (jamás hubo push). Desde este punto: implementadores dejan todo en working tree; checkpoints via ledger; review packages con git diff del working tree. Pendiente decisión del usuario sobre los commits existentes (dejar/aplanar/squash).
- DECISIÓN USUARIO: commits locales existentes SE QUEDAN (checkpoints; squash-merge final lo hace él). Rama remota del conductor SE QUEDA (respaldo; fue publicada por su editor, no por agentes). DESDE AQUÍ: modo working-tree — los agentes NO commitean; review packages via git diff del tree; el usuario commitea cuando quiera.
- P3-T4: COMPLETO (38c9880, review: Spec ✅, Aprobada). Byte-compat verificada (?? short-circuit + not.toHaveBeenCalled); SinVehiculoActivoError 400 coherente con middleware; OpenAPI propaga por referencia (schemas.ts .describe); edge de orden de errores = aceptable (adjudicado por reviewer). Backend P3 COMPLETO — faltan A5 (app, SIN commits) + E2E.
- P3-A5 implementador: COMPLETO en modo working-tree (SIN commits — 10 archivos modificados/nuevos en el tree del conductor sobre HEAD e73d3a6, incl. patch() en api_client agregado por el controller CON autorización del usuario). 5/5 tests, analyze 0 nuevos. Concern declarado: errorMessage del VM no se renderiza en vehicle_list_screen (fallo de "Usar este" sería silencioso). Pendiente review (package: conductor/.superpowers/sdd/review-worktree-p3a5.diff).
- P3-A5 review: Needs fixes → 1 Important CONFIRMADO y ampliado: "Usar este" sin feedback (errorMessage nunca renderizado en vehicle_list_screen + botón sin isSaving disable). Fix = ref.listen+SnackBar (patrón de driver_home:188-198) + onPressed null si saving. Fixer haiku despachado (working-tree, sin git). Lo demás limpio: fallback chain byte-exacta, patch() hereda refresh-401 gratis, mock no-op sano.
- P3-A5: COMPLETO (working tree del conductor, SIN commits; re-review CONFIRMADO). SnackBar verbatim del patrón driver_home + isSaving disable (previene PATCHes concurrentes). 10 archivos + test en el tree, listos para commit del usuario.
- P3-E2E: COMPLETO, DONE 11/11 sin fixes. Hooks T1/T2 en vivo (incl. autoasignación diferida al ganar rol) ✓ PATCH+gate cruzado ✓ derivación al aceptar ✓ SIN_VEHICULO_ACTIVO ✓ compat byte ✓ check10 por controller ✓ limpieza total ✓. NOTAS: (n1) viajes SIN ON DELETE CASCADE desde usuarios/conductores/vehiculos (preexistente — limpiezas futuras deben borrar viajes primero; ¿migración de cascade? → triage review final); (n2) 409 VEHICULO_NO_APROBADO intermedio fue comportamiento correcto. PENDIENTE: review final de rama P3.
## PARTE 3: COMPLETA. Review final (opus): lista para P4 "with fixes" = 1 item PROCEDURAL: el mod local del usuario en api_client.dart es un rewrite de sesión (~53 líneas: _sessionDead latch, cooldown refresh, _killSession) AUDITADO CORRECTO pero sin commitear — el usuario debe commitearlo antes de que P4 se apoye en él. Sub-nit opcional: _authStorage.clear() sin await en _killSession. TRIAGE: n1 cascade = DELIBERADO (soft-delete, NO agregar cascade) · m-P3s aceptados · m3/m5/m7/obs-T3 siguen como tickets. PARA P4: (p4a) columna origen en asignaciones · (p4b) nicety: revocar limpia id_vehiculo_activo si apuntaba al revocado · (p4c) evidencia E2E del caso positivo asignado-selecciona-vehículo-ajeno.
CHECKPOINT DE COMMIT DEL USUARIO: backend tree = jala-esquema-final.dbml (fix enums); conductor tree = 10 archivos A5 (vehículo activo app) + api_client.dart (su rewrite de sesión + patch() del controller) + test nuevo.

# Parte 4 — Bolsa de trabajo (2026-07-07, subagent-driven, MODO WORKING-TREE sin commits de agentes)
Plan: docs/superpowers/plans/2026-07-07-parte4-bolsa-trabajo.md (B1 migración 022, B2 CANDADO REVOCACIÓN regla del usuario + limpiar activo, B3 CRUD bolsa, B4 aceptar postulación tx+notifs, A5 app conductor, A6 app dueño, T7 E2E con cadena completa de la regla).
REGLA DE NEGOCIO NUEVA (usuario): no revocar asignación con viaje activo (conductor O vehículo) → 409 ASIGNACION_EN_VIAJE; al revocar OK limpiar id_vehiculo_activo si coincidía. Spec actualizado (§3.3). Plan y spec SIN commitear (el usuario commitea).
Gates decididos: publicar/gestionar=propietario · listar/postular=conductor.
- P4-B1: implementador DONE (022_bolsa_trabajo.sql en working tree, byte-idéntico al plan, aplicado + idempotente verificado; typecheck/22 tests verdes). Concern: 0 filas en asignaciones local → default 'propia' solo verificado estructuralmente. Pendiente mini-review. B2 despachado en paralelo.
- P4-B1: COMPLETO (022_bolsa_trabajo.sql en working tree, review Aprobada sin issues). En modo working-tree la atribución importa: B1 = solo ese .sql nuevo.
- P4-B2 implementador: DONE (working tree: 6 archivos M + 1 test nuevo — atribución exacta en su reporte). AsignacionEnViajeError 409; guard antes de revocar; cleanup no-fatal después; método nuevo conductorOVehiculoConViajeActivo en IViajeRepository (el existente conductorConViajeActivo no cubría OR vehículo; 1 solo implementador verificado). 25/25 + typecheck. Pendiente review.
- P4-B2: COMPLETO (working tree, review Aprobada; reviewer re-corrió suite 25/25 + tsc). Candado ASIGNACION_EN_VIAJE operativo end-to-end. Minor cosmético: el implementador no dejó parte4-task-2-report.md (el diff se verificó directo). ATRIBUCIÓN B2: revocarConductor.ts, flotillas/{errors,dependencies,openapi}.ts, IViajeRepository.ts, ViajePostgresRepository.ts, +revocarConductor.test.ts.
- P4-B3 intento 1: DETENIDO POR EL USUARIO a media tarea (dejó domain/ completo + crearVacante.ts con un if(!vacante) muerto según su propio self-review). RELANZADO (agente fresco continúa del parcial, instruido a borrar el dead code). 
- P4-B3 (reanudado): implementador COMPLETE (23 archivos src/bolsa + server.ts/docs.ts aditivos; 8/8 endpoints; 41 tests; dead code del parcial eliminado). Concerns: (c1) DELETE postulación responde 200 con recurso no 204; (c2) 23505 sin inspeccionar constraint name (un único índice violable por INSERT, precedente citado). Pendiente review.
- P4-B3: COMPLETO (review Aprobada; reviewer re-corrió tsc + 41/41). IDOR cerrado en los 3 ownership; PII recortada A MANO correctamente (persona() de viajes trae teléfono — reusarlo habría sido la fuga; nota para P5/panel); c1 200-con-recurso ACEPTADO; c2 23505 plano VALIDADO contra migración real. ATRIBUCIÓN B3: src/bolsa/* (23) + server.ts + docs/docs.ts.
- P4-B4 implementador: COMPLETO (47/47; tx opaca en BolsaPostgresRepository.aceptarPostulacion con withTransaction; asignar extendido {client?, origen?} idiom exec=client??pool, caller intacto; push real IPushSender/pickPushSender fallback Log; socket descartado deliberado; +404 consistente). Pendiente review con foco en el SQL interno de la tx (los tests no lo cubren).
- P4-B4: COMPLETO (review Aprobada; reviewer verificó SQL/locks/compat/push él mismo; 47/47+tsc re-corridos). BACKEND P4 COMPLETO (B1-B4). Minor→triage final: en aceptarPostulacion el sub-orden estado-antes-que-ownership filtra un bit (no-dueño distingue terminal vs pendiente vía 409/403) — comentario de 1 línea o swap; baja severidad. ATRIBUCIÓN B4: aceptarPostulacion.{ts,test.ts} + extensiones en bolsa/{errors,IBolsaRepository,BolsaPostgresRepository,dependencies,controller,routes,openapi} + asignaciones {IAsignacionRepository,AsignacionPostgresRepository}.
- SIGUIENTE: A5 app conductor (bolsa: ver vacantes + postular). OJO atribución: api_endpoints.dart ya está modificado por P3-A5 sin commitear — A5-P4 le AÑADE constantes (aditivo, reportar).
- P4-A5 intento 1: el agente se quedó OCIOSO esperando sub-agentes de investigación propios (anti-patrón). Reanudado con orden de implementación directa. LECCIÓN para dispatches: añadir "no spawn sub-agents; work straight-line" a los implementadores.
- ✔ EL USUARIO COMMITEÓ el checkpoint P3 del conductor (HEAD e73d3a6 → 7e3f548: P3-A5 vehículo activo + su rewrite de api_client). Working tree partió limpio para P4-A5.
- P4-A5 implementador: DONE (feature bolsa conductor completa: 2 secciones, cruce vacante↔mi-postulación por idVacante, 22/22 tests con 5 nuevos, analyze 0 nuevos, intersecciones aditivas). Concerns: re-postular tras retirar IMPOSIBLE (UNIQUE backend — ¿decisión de producto? → triage final); mis-postulaciones sin datos de vehículo si vacante cerró (fallback "Vehículo #id"); gate de rol solo en menú (403 backend cubre ruta directa). Pendiente review.
- P4-A5: COMPLETO (review Aprobada; cruce cliente correcto, mappers verificados contra los tipos TS reales, aditivos puros, isWorking disable heredado de la lección P3). Minors→triage: O(n) por card (escala ok); retirarPostulacion backend no valida estado previo (no explotable desde UI). PRODUCTO para triage final: re-postular tras retirar imposible (UNIQUE incondicional — ¿índice parcial o delete-on-retirar?). BONUS: vehicle_module.dart tiene un ../ de más que resuelve por normalización (bug latente preexistente — ticket).
- P4-A6 implementador: COMPLETO (28/28 tests, 6 nuevos; 4 pantallas/vm nuevos + extensiones aditivas a A5; verificó shapes contra backend real: postulacionesPendientes, sin data-wrapper en 3 endpoints, sin endpoint de rechazo individual confirmado). Decisión: entrada solo en perfil (vehicle_list = territorio P3/HEAD intacto). Concern: duplicación deliberada de constantes UI de estado (extension nueva vs switch de A5) para no tocar archivos A5. Pendiente review (package con baseline A5 para comparar).
- P4-A6: review Aprobada (A5 byte-idéntico verificado por diff directo; claims de backend verificados contra fuente; 27/27 real —el "28" del reporte era prosa). 1 Important → fixer: cast extra as Vacante no-nullable en app_router:81 (precedente vehicleDetail usa nullable+fallback). Minor: duplicación de constantes UI adjudicada aceptable con TODO. E2E P4 despachado en paralelo (backend).
- P4-A6: COMPLETO (fix del cast CONFIRMADO por re-review; analyze+tests re-corridos por el reviewer). IMPLEMENTACIÓN P4 COMPLETA (B1-B4, A5, A6) — solo falta E2E (corriendo) + review final.

# Parte 5 — Login cruzado + upgrade (2026-07-07, working-tree). Plan: docs/superpowers/plans/2026-07-07-parte5-login-cruzado.md
Secuencia por colisión: A1 pasajero AHORA (paralelo al E2E P4); B2 backend y A3 conductor esperan fin del E2E + checkpoint del usuario. Panel → P6.
- P5-A1 despachado (auditoría/quitar gates de rol en ViajeseguroApp).

# Parte 6 — Limpieza drop usuarios.rol (2026-07-07, working-tree). Plan: docs/superpowers/plans/2026-07-07-parte6-limpieza-rol.md
SECUENCIA DURA: X1-X3 backend esperan E2E-P4 + P5-B2/A3 + checkpoint. Diseño clave: /me SEGUIRÁ sirviendo campo `rol` = rolPrincipal(roles) (compat de lectura para apps/panel) + roles[]. X0 (explorador del panel, read-only en develop) DESPACHADO ya — único repo libre. Panel necesitará rama creada POR EL USUARIO (regla no-git).
- P5-A1: COMPLETO como AUDITORÍA LIMPIA (cero gates en ViajeseguroApp; evidencia: greps exhaustivos + trace splash/login/router role-agnostic + 16/16 tests incl. caso multi-rol del prep). Sin diff → sin review de tarea (adjudicación controller: nada que gatear; el E2E-P5 prueba login cruzado vivo por HTTP). Falta P5: B2 (backend activar) + A3 (pantalla upgrade conductor) + E2E — destrabadas al terminar E2E-P4.
- P6-X0: COMPLETO (panel explorado). CLAVE: el panel NO decodifica JWT — rol viaja en body de login/me → el diseño X1 (seguir sirviendo rol derivado en payloads) hace la migración INVISIBLE para el panel. Gates admin (useLoginViewModel:40, useCodeVerification:46) intactos. X4 reducida a: AuthUser.roles? opcional + verificación manual + eslint (puede plegarse al E2E si el usuario no quiere crear rama del panel). Sin suite de tests en el panel (solo eslint) — anotado.
- P4-E2E: COMPLETO 10/10 SIN FIXES. Cadena de la regla del dueño ✓ completa (409→completar→revocar 204→activo NULL→400). P3×P4 vivo ✓. Hallazgos: (e1) no existe camino de registro que produzca conductor-puro (el mapeo compat siempre da propietario) — by design P2, anotado; (e2) revocación devuelve 204 (convención preexistente; el plan decía 200 — prosa); (e3) shape público sin PII asertado. PENDIENTE: review final de rama P4.
## PARTE 4: COMPLETA. Review final (opus, 2 repos): YES — lista para checkpoint y P5/P6 encima. 0 Critical/Important. Concurrencia aceptar↔revocar SIN deadlock (verificado); push sin PII; pantallas null-UI-safe; OpenAPI==código 9/9. TRIAGE: t1 TICKET-barato (reorden ownership-antes-que-estado en aceptarPostulacion, 2 líneas) · t2 DECISIÓN DE PRODUCTO al dueño (re-postular tras retirar: hoy imposible por UNIQUE incondicional; enable-path = índice parcial WHERE estado<>'retirada') · t3 TICKET-barato (guard estado previo en retirarPostulacion) · t4/t6/t7 ACCEPT · t5 ticket aparte preexistente · m-vivos siguen no-bloqueantes. PLAN: foldear t1+t3 con un fixer cuando P5-B2 libere el backend, ANTES del checkpoint del usuario.
- DECISIÓN DE PRODUCTO (usuario): t2 = SÍ re-postular tras retirar. Diseño elegido (ponytail): REVIVE por UPSERT en crearPostulacion (ON CONFLICT DO UPDATE SET pendiente WHERE estado='retirada'; RETURNING para detectar conflicto no-retirada → 409 igual). SIN migración. Spec §3.3 actualizado. PLAN DE FOLDEO: cuando P5-B2 libere el backend → UN fixer bundle (t1 reorden guards + t3 guard estado en retirar + t2-revive + tests); cuando P5-A3 libere el conductor → mini-fixer app (botón "Postularme de nuevo" si retirada). Todo ANTES del checkpoint del usuario.
- P5-A3 implementador: DONE (10 archivos: pantalla+vm upgrade, gate en splash Y login —el splash no tenía el gate por roles, corregido—, activarPropietario en profile repo/api/mock, aditivos endpoints/rutas). 27/27, analyze 0 categorías nuevas. Pendiente review. Conductor libre → mini-fixer t2-app despachado en paralelo (archivos distintos).
- P5-B2 implementador: DONE (6 archivos; ruta activar ANTES del use() del gate — decisión Express-order, sin sub-router; 49/49). Backend libre → bundle fixer t1+t3+t2-revive despachado. Conductor libre → mini-fixer t2-app. Reviewers B2+A3 en paralelo (read-only).
- t2-app: HECHO (bolsa_screen.dart:163-174, chip+botón solo en retirada, 11/11). Quedan en vuelo: reviewer B2, reviewer A3, bundle fixer backend (t1+t3+t2-revive).
- P5-B2: COMPLETO (review Aprobada; orden Express verificado in-situ, sin weakening; idempotencia delegada a UPSERTs existentes; use case dumb-wiring correcto; 49/49 re-corrido). Quedan: reviewer A3 + bundle fixer backend.
- P5-A3: review Aprobada (refresh fiel a la plantilla, gates correctos, logout real verificado vía override de main.dart:63). HALLAZGO elevado por el controller a fix: refresh silencioso post-activar (activó server-side pero navega con token viejo → banner confuso). Fixer haiku despachado (contrato ok/okSinRefresh/error + mensaje 'cierra sesión y vuelve a entrar'). Minors: infos de lint conocidos.
- Fix refresh-silencioso A3: HECHO (ActivationResult{ok,okSinRefresh,error}; pantalla ramifica con SnackBar naranja + logout; 27/27). SOLO queda el bundle fixer backend → luego E2E-P5.
- Bundle t1+t3+t2-revive: HECHO (52/52; guards 404→403→409; retirar WHERE pendiente + PostulacionNoPendienteError reusado; UPSERT revive con RETURNING y catch 23505 eliminado-muerto; OpenAPI actualizado). NOTA: evidencia P4-step4 (re-postular→409) superseded → E2E-P5 la re-verifica viva. Fix1/Fix3 sin test SQL-level (patrón del módulo, mocks) — cubre E2E.
- E2E-P5 despachado (todo libre).
- P6 inventario (read-only, paralelo al E2E-P5): DESPACHADO — mapa exhaustivo de usos de usuarios.rol (JWT/SQL/respuestas/tests/fallbacks) → worklist literal para X1/X2. Implementación X1-X3 sigue bloqueada hasta E2E-P5 verde + checkpoint.
- P6 inventario: COMPLETO. Worklists X1/X2 escritas en el plan con file:line verificados. Hallazgos clave: (i) editarPerfil/uploadPhoto no cargan roles[] → firma nueva toServingUserJSON(user, roles) como punto único de fan-out (userView:10-15); (ii) seeds 001_admin.sql usa la columna 3 veces → reescritura; (iii) fallbacks ?? [rol] SOLO en authMiddleware:31 y socketServer:37; (iv) registrationToken.rol = intención, FUERA de alcance; (v) UserPostgresRepository único repo que toca la columna. P6 lista para ejecutar tras E2E-P5 + checkpoint.
- RECORDATORIO ACTIVO: al terminar P6 y escribir handoff-contexto.md → AVISAR EXPLÍCITAMENTE al usuario "listo para /compact" (él espera la señal; no compacta hasta que yo avise).
## PARTE 5: COMPLETA. Review final (opus, delta): YES, 0 Critical/Important. Bundle verificado a fondo (UPSERT race-safe con serialización Postgres; FK 23503 nunca estuvo en scope; guards 404→403→409 con FOR UPDATE primero). Minors: comentario obsoleto ARREGLADO por controller (bolsa_viewmodel:67-69); "logout no-op" = FALSO POSITIVO (main.dart:63 overridea con authSessionServiceProvider real — verificado). Informational: activar 2-awaits no-tx aceptado (idempotente+self-heal).
- SIGUE: checkpoint del usuario (P4+P5 en ambos árboles) + P6-X1 despachado en paralelo (archivos X1/X2 DISJUNTOS del trabajo sin commitear — atribución limpia).
- ✔ CHECKPOINT DEL USUARIO HECHO: backend 5826be0 (todo P4+P5) · conductor df791c0 (app P4+P5; mensaje dice "candado" — cosmético, avisado). Árboles limpios pre-X1. X1 EN CURSO (su huella = los ~11 archivos vivos). Panel: rama AÚN NO creada (sigue develop) — pedido de nuevo al usuario; X4a bloqueada solo por eso.
- Rama del panel creada POR EL CONTROLLER con autorización explícita del usuario ("crea tú la rama pero no hagas commits"). X4a despachada (tipo tolerante + eslint, working-tree).
- P6-X4a: HECHA (panel session.types.ts:6-7 roles?: Rol[] + lint limpio, working-tree sin commits). Panel COMPLETO para P6 (verificación manual → plegada al E2E-P6). Solo queda backend: X1 (en curso) → X2 → X3 → E2E.
- P6-X1 implementador: COMPLETO (51/51; SessionResponse +roles[] decisión propia razonable — confirmar tolerancia en E2E). NOTA: el commit 5826be0 del usuario absorbió 3 archivos X1 ya editados (jwt, authMiddleware, su test) — nada perdido, historial mezclado cosmético, usuario avisado. Pendiente review X1 → X2 (secuencial: comparte User.ts/completeRegistration).
- P6-X1: COMPLETO (review Aprobada, 0 issues; 51/51+tsc re-corridos; absorbidos verificados; rol derivado consistente con el token en los 4 auth). Sigue X2.
- P6-X2: implementador COMPLETO (51/51; RolUsuario eliminado sin importadores; seed validado empíricamente en BD scratch ×3). 🐛 BUG REAL CONFIRMADO POR CONTROLLER: createAdmin NO inserta usuario_roles (grep=0) → admin invitado pierde admin al primer refresh. LECCIÓN DE PROCESO: reviewers de P2-B1 y P4-final AFIRMARON un insert inexistente (verificación alucinada); la lectura directa de X2 lo destapó — m7 (guard roles vacíos) habría cazado esto en runtime: SUBIR m7 a fix en X3. PLAN X3 ampliado: fix createAdmin (+test) + migración 023 = sweep final (INSERT usuario_roles desde usuarios.rol ON CONFLICT NOTHING, última vez antes del drop) + DROP COLUMN + DROP INDEX + guard m7 en emitirTokens (log/error si roles vacío).
- P6-X2: COMPLETO (review Aprobada, 0 issues; constructor verificado campo-a-campo; 51/51+tsc re-corridos). Solo queda X3 (en curso) → E2E-P6 → review final → HANDOFF.
- P6-X3: implementador COMPLETO (createAdmin+INSERT rol vía exec/client; guard m7 con test barato; MIGRACIÓN 023 APLICADA — \d usuarios SIN rol; 52/52 post-drop). Cobertura fix-1 diferida a E2E-P6 (sin harness de repo — decisión documentada). Reviewer X3 + E2E-P6 despachados en paralelo.
- E2E-P6 intento 1: delegó a hijo y quedó ocioso (2ª vez del anti-patrón — P4-A5 fue la 1ª). Reanudado en directo. REGLA DE DISPATCH permanente: "NO sub-agents, NO sleep/wait, straight-line" EXPLÍCITO en todo implementador/E2E.
- P6-X3: COMPLETO POR ADJUDICACIÓN CON EVIDENCIA. Review dijo "needs fixes" = solo evidencia faltante (mi dispatch omitió el report file — error del controller); CONTROLLER la levantó: _migrations registra 023 @22:55 ✓ · information_schema sin columna rol ✓ · sin idx ✓ · reporte 52/52 con desglose ✓. Código: 0 issues (3 archivos ~15 líneas, orden sweep-antes-drop correcto, tx atómica, guard con assert real de console.error). Hard-gate del reviewer → ADDENDUM enviado al E2E (assert SQL directo de la fila admin). Solo queda: E2E-P6 → review final P6 → HANDOFF → 🔔 compact.
- P6-E2E: PASÓ ×2 (run + replicación, ids 23-26). BUG PROOF con SQL assert ✓ · m7 log literal ✓ · panel shape ✓ · seeds post-drop ✓. Concerns ambientales anotados (claves efímeras por sesión — nota de deploy). PENDIENTE: review final P6 → HANDOFF → 🔔.
## PARTE 6: COMPLETA. Review final (opus): código completo/coherente; único fix = OPERATIVO (deploy código-antes-de-023, runbook en handoff §5.4). Token-window = histórica, TTL-cerrada, verificada empíricamente. m3 RESUELTO; residuales aceptados.
# ═══ PROYECTO MULTI-ROL + BOLSA: IMPLEMENTACIÓN COMPLETA (6/6 partes, 4 repos) ═══
HANDOFF ESCRITO: .superpowers/sdd/handoff-contexto.md (con veredicto+runbook). Usuario AVISADO para /compact. Post-compact: leer handoff+ledger → pasada MINIMALISMO app conductor (lógica intacta) → prueba manual → merge único → deploy por runbook.

# Minimalismo + espacialismo app conductor — 2026-07-08 (inline, sin commits)
Aprobado por Edward vía AskUserQuestion: stats→panel inferior · zonas solo círculos (sin badges/intensidad/detalle) · quitar card vehículo del perfil · borrar profile_screen.dart + "Centro de ayuda" (resolveDocumentsRoute ya no existía).
- Base: JalaBrand.success en theme; GradientButton aplanado a FilledButton del theme (16 pantallas); snackbars rojos→scheme.error (7 archivos, sed).
- Inicio 1114→733 líneas (script por rangos scratchpad/minimal_home.py): sin franja stats (resumen "$X hoy · N viajes · Xh" en bottom sheet), sin badges de zonas ni sheet de métricas técnicas, sin card de carga, _TripCard plano (Card theme, sin barra lateral/chips), estado vacío sin caja, verdes tokenizados.
- Perfil: rol·estado en una línea (sin badge-caja), sin card vehículo, sin Centro de ayuda, avatar/estado tokenizados, padding 24/aire 32.
- Flotilla e Historial: reescritos a theme (arregla dark mode; usaban paleta hardcodeada con cards blancas fijas); flotilla: sin header "TUS VEHÍCULOS (n)", facturación al final, badge única señal de estado (En uso > estado).
- Router: -ProfileScreen (ruta+const+archivo, 430 líneas muertas). Bolsa/upgrade: solo snackbar fix (nacieron con theme).
- Diff: 14 archivos, +416/−1440 (neto −1024). flutter analyze lib: 0 errores (11 infos preexistentes en archivos no tocados). Review subagente (sonnet): APROBADO, 0 Critical/Important. Working tree — Edward commitea.
- OLA 2 (pantallas secundarias, 3 subagentes sonnet en paralelo + review): registro/login (8 archivos, OTP hereda InputDecorationTheme, photo tokenizada, resto ya cumplía) · documentos (5, mapa semántico aprobado/rechazado/revisión/pendiente tokenizado, cards del theme, CTA Continuar→GradientButton misma condición) · ganancias (header sin caja ámbar→jerarquía tipográfica, filas estilo historial, _Chip muerta borrada) · viaje en curso (solo panel: tokens+textTheme; mapa/sockets/botones de fase intactos). Diff ola 2: 13 archivos +355/−450. flutter analyze lib 0 errores (11 infos preexistentes). Review (sonnet): APROBADO 0 hallazgos. Working tree — Edward commitea.
- Hallazgo preexistente (fuera de alcance, chip creado): document_view_screen.dart no renderiza la imagen real del documento (icono placeholder estático; AuthedImage no se usa ahí).
- LOGIN REDISEÑADO CON FIGMA (node 8:4 "App Pasajeros", frame Registro-numeroTelefono como referencia): lockup mototaxi SVG desnudo (disco crema solo en dark) + wordmark Jala displayMedium, título/caption alineados a la izquierda, campos sin prefix icons, CTA fijo al fondo (bottomNavigationBar + viewInsets, sube con teclado). GradientButton volvió a ÁMBAR de marca con texto blanco (global, per Figma: todos los CTA naranjas). Lógica _onLogin/gates intactos. analyze 0 errores. Working tree.
- GETSTARTED REDISEÑADA CON FIGMA (frame 99:4): pill ámbar + wordmark Jala 72px izq + hairline + claim "Tu mejor opción para el pueblo" + ilustración de ruta (assets nuevos descargados del Figma: getstarted_ruta.svg + mototaxi_map.svg, limpiados de preserveAspectRatio=none) con 2 chips flotantes (_FloatChip, sombra ámbar) — ilustración solo portrait, Spacer en landscape. GradientButton ganó param opcional `icon` (Comenzar →). CAMBIO DE PRIORIDAD DE CTA per Figma: primario "Comenzar"→registro, "Ya tengo cuenta · Iniciar sesión" como link (antes login era primario) — ambas rutas siguen accesibles; Edward puede revertir con swap trivial. analyze 0 errores.
- GETSTARTED v2 (Fable directo, mejora sobre el Figma, no calca): wordmark Jala 64px + tag CONDUCTOR ámbar (identidad real de la app) · claim para choferes "Conduce por tu pueblo, gana a tu ritmo." (acento amberDeep/amberLight según brightness) · escena ruta+mototaxi con chips REORIENTADOS al conductor ("Nuevo viaje / Centro → 5 de mayo" y "Ganancias de hoy / $450") · animación de entrada (fade+slide 900ms escalonado) + flote del mototaxi (2.8s reverse) con respeto a MediaQuery.disableAnimations · pill/divider del Figma eliminados (clunky) · landscape: sin escena, wordmark 44. StatefulWidget puro (se quitó ConsumerWidget con ref sin uso). analyze 0 errores.
- GETSTARTED v3 (feedback Edward: mototaxi SVG se veía "barato"): SIN ilustración — propuesta de valor tipográfica: 3 filas (Viajes cerca de ti / Ganancias claras / Bolsa de trabajo) con icono en círculo ámbar suave, entrada escalonada (un solo controller, intervals). Assets figma getstarted_ruta.svg y mototaxi_map.svg ELIMINADOS (sin referencias). analyze 0 errores.
- REGISTRO UNIFICADO (mismo lenguaje que login/portada): nuevo paso_registro_scaffold.dart (eyebrow "PASO X DE 6" en acento + título/caption izq + contenido scroll + CTA fijo abajo con viewInsets + belowCta opcional). Los 6 pasos reescritos sobre la plantilla (email sin prefix icon, otp con reenviar como belowCta, names, datos personales con Radio deprecado intacto, municipio, foto con omitir como belowCta). Lógica byte-idéntica. + FIX DEVICE PREVIEW: app.dart leía brightness de View.platformDispatcher (SO real) → ThemeMode.system con MediaQuery heredado; ahora el toggle claro/oscuro de DevicePreview funciona. analyze 0 errores.
