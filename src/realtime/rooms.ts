export const usuarioRoom = (id: number): string => `usuario:${id}`;
export const conductorRoom = (id: number): string => `conductor:${id}`;
export const municipioRoom = (id: number): string => `municipio:${id}`;
